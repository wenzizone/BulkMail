P19
在一个页面中交换图片
在这个小技巧中，我们通过更新图片运行时的属性来交换页面中的一个图片。
1.在当前项目中添加一个表Recipe5.aspx
2.在CSS中添加一个如下的图片控制类
3.为刚刚的表添加一个图片控制，设置其CSS类为刚刚创建的图片控制类
4.添加一个按钮控制来触发图片交换这个事件
这样，一个完整的ASPX图表标记就完成了，如下：
P20
加载后，页面中的图片显示如下：
我们现在可以通过单击按钮来交换图片了
P21
1.在document.ready（）函数中初始化时使按钮有效
2.通过bind方法为按钮定义一个回调函数
3.放弃默认表的提交
4.通过使用src属性来更新图片的URL
5.通过添加disabled属性来使按钮失效
这样，完整的jQuery解决方案如下：
P22
运行这个WEB页面。点击Swap Image按钮。图片会进行更新，显示如下：
将鼠标移到上面改变图片的透明度
裁剪页面中的图片
jQuery提供了很多方便的插件来增强对于WEB元素的控制。对于需要图片裁剪的应用，一个叫做jCrop的插件可以用来控制图片。在这个小技巧中，我们将用这个插件来进行图片的裁剪并显示被裁剪的部分。
P23
1.在当前项目中添加一个表Recipe6.aspx
2.从http://deepliquid.com/content/Jcrop.html下载JCrop插件
3.将jquery.Jcrop.js文件保存到当前的js文件夹。保存样式表文件jQuery.Jcrop.css到当前的样式表文件夹css
4.添加刚刚的两个文件到页面中并添加jQuery的库。
5.添加用于图片控制的css类
P24
6.创建如下的ASPX图表标记。
这样，加载后，页面显示如下：
P25
1.在document.ready（）函数中的图片控制中调用插件的JCrop方法
2.在onChange事件中指明回调函数
P26
3.在onSelect事件中指明回调函数
4.定义回调函数。在回调函数镇南关传递事件对象e
5.（e.x,e.y）表示裁剪区域的左上角坐标，其值显示在（X,Y）文本框中。
6.（e2.x,e2.y）表示裁剪区域的右下角坐标，其值显示在（X2,Y2）文本框中。
7.e.w表示裁剪区域的宽度，显示如下
8.e.h表示裁剪区域的高度，显示如下
这样，完整的jQuery解决方案如下：
P27
运行这个页面。点击图片的任何区域然后拖拽光标根据要求来选择裁剪区域。被裁剪的部分和保留部分都显示在页面中。
页面中图片交换
P28
创建一个图片浏览器
在这个小技巧中，我们借助jQuery来创建一个图片浏览器。
1.在当前项目中添加一个表Recipe7.aspx
2.在表中添加一个div区域，并在此区域中添加多个图片控制
3.在这个div区域中添加如下的css样式
4.添加如下的css样式到图片控制中。注意div区域的图片位置定义为：absolute并且左上角的位置固定。
P29
5.添加按钮来导航到下一个和上一个
这样，一个完整的ASPX图表标记就完成了，如下：
P30
1.在document.ready（）函数中定义一个变量z来表示图片的z轴
变量z指的是一个元素在z轴上的位置。它决定一个元素的深度信息。一个拥有较小深度的元素会被较大深度的元素所覆盖掉。
2.假设图片可以叠加到其他图片上，那么最大可能的深度就等价于当前页面可以控制的图片数目
在div区域的每一个图片控制都唯一地制定一个z-index，其值可以为1到max_z。拥有最大的max_z将会覆盖所有的其他图片，也就是当前的显示的图片
3.循环div区域的图片
4.在循环中为是下一个图片的z比上一个图片加1
5.使z-index对应到图片的z值
6.这样，运行页面，将会显示当前拥有最大z-index的图片8，如下图所示
点击前一个按钮我们将会使每一个图片控制的z-index减1.z-index为1的图片将变为拥有max_z的图片，这样这个图片就成为当前显示的图片。
7.定义前一个按钮的单击事件
8.放弃默认表的提交
9.循环div区域的每一个图片控制
10.在循环中获得当前图片的z-index
P32
11.如果其值为最小值如1，那么将其置为max_z
12.其余地逐一对其z-index减1
点击下一个按钮我们将会使每一个图片控制的z-index减1，当前的图片（其值为max_z）将会变为1，这样就放到了栈底。
13.定义下一个按钮的单击事件
14.放弃默认表的提交
15.循环div区域的每一个图片控制
16.在循环中获得当前图片的z-index
17.如果其值为最大值如max_z，那么将其置为1
18.其余地逐一对其z-index加1
完整的jQuery解决方案如下：
P34
运行这个web页面。单击导航按钮。页面将会按要求来显示前一个或者下一个页面。
鼠标放在缩略图上放大图片
鼠标悬停放大图片
在这个小技巧中，我们将通过鼠标的悬停来放大图片。放大的倍数可以按要求来指定。
P35
1.在当前项目中添加一个表Recipe8.aspx
2.在表中添加一个div区域，并在此区域中添加多个图片控制
3.div区域的css样式表定义如下
4.图片的css样式定义如下
这样，一个完整的ASPX图表标记就完成了，如下：
P36
加载页面，页面显示如下：
1.在document.ready（）函数中指定要放大的倍数，在这个例子中我们将其放大10%
2.指定图片放大后左边和上边可以移动的数量
3.为图片指定hover事件
4.在mouseenter事件的回调函数中找到放大后的宽度
5.找到放大后的高度
6.给图片的宽度、高度、上、右属性赋值，持续事件赋值为200ms
7.在mouseleave事件的回调函数中，保存图片原来的信息。用div区域的容器来保存图片的原来信息，宽度、高度、左、右属性激活时间为100ms
这样，完整的jQuery解决方案如下：
P38
运行页面。鼠标悬停在图片上，你会注意到图片放大了，如图所示：
鼠标离开后，图片恢复原样。
在页面中交换图片。